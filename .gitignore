# ---------------------------------------------------------------------------
# Bitrix runtime — никогда не должно попадать в репозиторий модуля
# ---------------------------------------------------------------------------
# (если случайно оказалось в www/local/modules/my.tiptap/, не коммитим)
*.log
*.tmp

# node / сборка фронта
node_modules/
package-lock.json
.DS_Store
Thumbs.db

# редакторы
.idea/
.vscode/
*.swp
*.swo
